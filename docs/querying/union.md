---
sidebar_position: 6
title: Union Queries
---

# UNION Queries

Sequelize provides a way to combine the results of multiple `SELECT` queries into a single result set using the `UNION` operator. This is achieved using the `sequelize.union` method.
